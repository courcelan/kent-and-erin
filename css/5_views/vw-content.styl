.vw-content--tier3
    .ctrlbar--cnd:first-child
        +under(breakpoints.condensed)
            padding: blh(.5)
            border: 0
            background: linear-gradient(to bottom, wh(100), darken(wh(100), 3%) 50%)
            filter: drop-shadow(0 1px 2px rgba(#000, .25))

    .nav-local + *
    .copy + *
        +under(breakpoints.condensed)
            border: 0
